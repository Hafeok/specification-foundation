# Task record — scrub and package the category derivation bundle

**Recorded before the first act, 2026-09-04.** A bounded task inside the `CG-R-35` hold, not a
new gate; Gate 2 remains held. Issued by Emil as `task-scrub-and-package-bundle.md`, sha256
`7784cadc977f0c61fe98fcb9b4df56aded5f7ad2cfbd62a1810b28a2fc526b98`, 83 lines, filed byte-identical under `inputs/`.

## Ruling basis

- **`CG-R-35`** — the list is confirmed by blind re-derivation; the receiving session gets the
  grid, the act type and the admission conditions and nothing else.
- **`CG-R-39`** — a subagent does not count, and the **framing channel** is named: framing is how
  a list gets fitted without anyone intending it. This task is that channel found open in a
  path name: `blind-rederivation/` says there is a prior derivation and a comparison.
- **`CG-rule-02`** — the rename is a change, not a rewrite: `git mv`, no force-push, the filing
  commits stand.
- **`CG-rule-09`** — the leak is an error of this session's, attributed here: the bundle was
  built to exclude the set and the list and was named after the design it was built to hide.

## Gates

| Gate | Act | Holds for |
|---|---|---|
| A | leak audit — enumerate every hit with what a reader could infer; fix nothing; propose a neutral folder name | ratification of the audit and the name |
| B | `git mv`; apply ratified substitutions, neutral wording never deletion; refuse any that would change what the derivation asks; regenerate the manifest; add `INVOCATION.md`; file the scrub record outside the bundle | ratification |
| C | archive with sha256 in the session log; open the PR as provenance record, not delivery | ratification; then back to the `CG-R-35` hold |

## Prohibitions carried

The proposed list, the Gate 1 set and the reconciliation rule are not touched. The scrub record
never enters the bundle. The receiving session gets an archive, never repository access.
