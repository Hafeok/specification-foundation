# Invocation — notation falsifier pre-registration

The message that started this session, delivered by Emil on 2026-09-03 with three attachments:
the session prompt (`session-notation-falsifier-prereg.md`), the notation pre-registration
(`prereg-notation-inference-load.md`), and the bundle (`notation-falsifier-bundle.zip`). Filed
verbatim. Where it and the prompt differ, the prompt is the instruction set; no difference was
found (see `gate0-report.md`).

---

> Produce the pre-registration artefacts for the notation falsifier.
>
> Attached: notation-falsifier-bundle.zip. Read session-notation-falsifier-prereg.md — it is the
> instruction set. Inputs under inputs/: the notation pre-registration (9d29b2fd…), the layer
> pre-registration for contrast (36c5b767…), and the binding under test.
>
> Before Gate 0, read Hafeok/canon-governance at \<REF\> and record the ref. Its rules/ directory
> is in force here and is not restated in the bundle. Two of the ten are provisional.
>
> This session proposes only. Four artefacts: the act and its determination set, the category
> list, quantities and thresholds, the classification rubric. It authors neither arm, builds no
> harness, and runs nothing. Each gate holds for explicit ratification.
>
> The claim under test is the notation claim — holding determinations constant, a conforming
> notation leaves less to inference than prose carrying the same content. It is not the layer
> claim and not the accrual claim; the layer pre-registration is included so the distinction is
> visible, not as work to do.
>
> Branch claude/notation-falsifier-prereg-\<suffix\>. Session-neutral identity Claude
> \<noreply@anthropic.com\>. Prompt and bootstrap to meta/sessions/ as the first act.

---

**One placeholder arrived unfilled.** `<REF>` was not replaced with a commit. The session read
the head of `canon-governance`'s default branch and recorded that hash in `bootstrap.md`; the
choice, and what differs between it and the ref this repository's README pins, is reported at
Gate 0.
