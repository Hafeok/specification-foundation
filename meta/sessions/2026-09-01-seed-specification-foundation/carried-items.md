# Carried items

Items this session identified and deliberately did not act on, carried so they do not evaporate
with the session. Distinct from `outstanding-dependencies.md`, whose entries blocked session work
and are cleared; these outlive it.

## CI-1 — session output that is not a session record *(defect, home named, unruled)*

**The falsifier session's Gate 0 report was never a repository object.** Its prompt instructed
"produce, without writing anything to the repository", the session correctly obeyed, and the most
substantive artefact it produced exists only as chat output — its content surviving refracted
through a draft that responds to it finding by finding
(`evidence/falsifier/rulings-gate0-closure-falsifier.md`).

This is a live defect in the session convention, not a curiosity: **CG-rule-06 requires session
records; nothing requires session output to be one.** A gate that produces analysis and holds
without writing produces something no later reader can retrieve. The instruction came from the
principal's own prompt, and the defect is owned there on the record
(`rulings-gate4-close.md`).

**Intended home:** a proposal against `canon-governance` — *Gate 0 output is filed even when the
gate produces no repository structure.* Deliberately not fixed inside this session's closing
gate; not ruled at close. This seed's own practice is prior art for the proposal: every gate
output here is a committed file.

## CI-2 — the scope question, at seven instances *(decision awaited, outside any gate)*

**Nothing checks the governing acts.** Seven recorded instances, every one caught by a person
reading a document after changing the state it described; no instrument, no rule. The set:
the four found during the `canon-governance` seed; E14's coverage limit (fifth, `CG-R-15`); the
CG-R-14 sidecars being prose (sixth, the Gate 3 ratification); the Part B addendum's prose guard
on the draft-without-its-report (seventh, counted at close, 2026-09-03).

It is the best-evidenced open question in the framework and it is not converging by accumulating
instances. The decision it awaits, in the principal's words: **an instrument, or an explicit
ruling that there never will be one** — leaving it open indefinitely being the third option, the
one that happens by default. The decision belongs outside a gate and to the ruling authority;
this entry exists so the set and its count survive the session that last extended it.
