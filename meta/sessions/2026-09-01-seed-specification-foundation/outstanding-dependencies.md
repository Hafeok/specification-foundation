# Outstanding dependencies

Dependencies this session cannot discharge itself, recorded per the Gate 2 acknowledgement as
entries rather than notes. Each names its owner and what it blocks. Removing an entry requires
the dependency actually landing, with a pointer to where.

| # | Dependency | Owner | Blocks | State |
|---|---|---|---|---|
| OD-1 | **Input 8** — the prior falsifier session's Gate 0 output, from `claude/closure-falsifier-prereg-w1mm7d`. Not reconstructed here, per the invocation. When it arrives it also anchors the pre-registration's hash (`145ed0a2…`, matching the delivered copy) | ~~Emil~~ → the session (ownership corrected at close: the branch was reachable only by the session) | **Gate 3** — placement of the falsifier material, and the one available cross-check of the five unanchored inputs | **cleared, 2026-09-02** — retrieved from `Hafeok/product-cli@a1232bf2…`; arrival record filed, prereg hash verified at source, and the finding that the Gate 0 report was never a repository object recorded, not repaired (arrived-inputs addendum; `gate3-partb-addendum.md`) |
| OD-2 | **The ruling-register append** — CG-R-10, -11, -12 (Gate 1), CG-R-13, -14, -15 (Gate 2) and CG-R-16 (Gate 3) are owed rows in `canon-governance`'s `registry/rulings.yaml`, the register the next ruling must append to (CG-R-8). Until the append, **seven** rulings exist only as this repository's verbatim filings (`rulings-gate1.md`, `rulings-gate2.md`, `rulings-gate3.md`) — a downstream repository as sole record of programme rulings, which is exactly the state the register exists to end. *(Amended from six at Gate 3 ratification: "OD-2 is mine and I will clear it … CG-R-16 makes seven.")* | ~~Emil~~ → split at close: Emil drafted, the session applied ("What I can do is draft the rows. Applying them is yours") | nothing in this session mechanically; every future reader of the register, materially | **cleared, 2026-09-02** — appended at `canon-governance@c5383be`, subjects and notes verbatim, field mapping to the register's existing schema stated in the section comment. Note: this append moved `canon-governance`'s head; the README pin stays at `ad6d1b0b…` per CG-R-10 — it records what the rules read were read at, and the append touched only the register |
